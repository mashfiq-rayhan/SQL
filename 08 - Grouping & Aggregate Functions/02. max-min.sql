-- SELECT MAX(amount_billed) AS max_bill, MAX(amount_tipped) AS max_tip
-- FROM bookings;

-- SELECT MIN(category), MAX(category)
-- FROM tables;

-- SELECT MIN(booking_date), MAX(booking_date)
-- FROM bookings;